You are acting as a Senior Performance Engineer, Debugging Specialist, Frontend Camera Expert, and Production SaaS Architect.

IMPORTANT:
This is an already working application.
Do NOT rebuild or redesign the system.
Do NOT delete working features.
Only enhance performance, stability, debugging, and fix the webcam preview.

Main Goal:
Transform the existing MVP into a fast, stable, production-ready SaaS app with a working live webcam preview.

────────────────────────────
TASK 1: PERFORMANCE AUDIT
────────────────────────────
Analyze the entire codebase and detect:

1) Slow operations
2) Unnecessary frontend re-renders
3) Heavy API calls
4) Inefficient image processing
5) Blocking synchronous logic
6) Memory leaks
7) Large bundle sizes

Output:
- Bottlenecks
- File locations
- Impact level (High / Medium / Low)

────────────────────────────
TASK 2: FRONTEND OPTIMIZATION
────────────────────────────
Improve performance without redesigning UI.

Add:
- Lazy loading for heavy components
- Image compression before upload
- Loading skeletons
- Error boundaries
- Retry logic for failed API calls

Do NOT change the design.

────────────────────────────
TASK 3: CAMERA LIVE PREVIEW FIX
────────────────────────────
Problem:
Webcam connects but no live preview is visible.

Fix the webcam preview safely:

1) Ensure video element:
- Has autoplay
- Has playsinline
- Correct binding to stream

2) Fix getUserMedia logic:
- Proper async handling
- Error catching
- Assign stream to video.srcObject
- Call video.play()

3) Add debug logs:
- Camera permission status
- Device selected
- Stream started
- Video element ready

4) Add fallback:
If specific camera (like Iriun) is not found:
→ Use default camera automatically

5) Prevent:
- Black screen
- Frozen preview
- Multiple streams

────────────────────────────
TASK 4: CAMERA & IMAGE PIPELINE STABILITY
────────────────────────────
Enhance capture logic:

- Detect camera disconnect
- Retry capture if failed
- Prevent blank/corrupted images
- Add capture timeout
- Standardize image size

Log:
- Camera start
- Capture success
- Capture failure

────────────────────────────
TASK 5:
